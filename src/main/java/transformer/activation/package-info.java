/*
 * transformer-from-scratch
 * MIT License — see LICENSE file at project root
 */

/**
 * Activation functions.
 *
 * <p>{@link transformer.activation.ActivationFunctions} provides ReLU and GELU
 * (tanh approximation) via a {@code Type} enum, with both scalar and matrix
 * entry points.
 */
package transformer.activation;
