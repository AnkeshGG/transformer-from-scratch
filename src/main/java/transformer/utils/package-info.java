/*
 * transformer-from-scratch
 * MIT License — see LICENSE file at project root
 */

/**
 * Math and initialisation utilities.
 *
 * <ul>
 *   <li>{@link transformer.utils.MathUtils} — numerically stable softmax,
 *       log-softmax, and argmax over {@code float[]} vectors.
 *   <li>{@link transformer.utils.RandomUtils} — Xavier-uniform weight
 *       initialisation for 1-D and 2-D arrays, and seeded RNG creation.
 * </ul>
 */
package transformer.utils;
